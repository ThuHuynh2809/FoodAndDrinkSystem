spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NguyenDangDucTai/springboot-config-server
          clone-on-start: true
        bootstrap: true
      enabled: true

  application:
    name: Config-Server
